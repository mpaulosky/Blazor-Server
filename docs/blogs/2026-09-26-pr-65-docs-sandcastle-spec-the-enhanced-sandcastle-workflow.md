---
post_title: "docs(sandcastle): Spec the enhanced Sandcastle workflow"
author1: mpaulosky
post_slug: "v0.0.44-pr-65"
microsoft_alias: n/a
featured_image: ""
categories:
  - engineering
tags:
  - release:v0.0.44
  - automation
ai_note: "Generated by release automation from the PR title, description, commits and changed files. No AI summary."
summary: "Release notes seed for v0.0.44 from PR #65."
post_date: "2026-09-26"
---
## docs(sandcastle): Spec the enhanced Sandcastle workflow

- **Release tag:** `v0.0.44`
- **Source PR:** [#65](https://github.com/mpaulosky/Blazor-Server/pull/65)

### PR description

## Summary

Adds the design spec for the enhanced Sandcastle pipeline, charted on [Map: Enhance the Sandcastle issue workflow](https://github.com/mpaulosky/Blazor-Server/issues/52):

- `docs/plans/sandcastle-workflow.md`: labels, the six-phase round (follow-up sweep → intake → blocker gate → plan → critique → build), the role team, `scripts/gate.sh`, models and budgets, giving up, and the Actions trigger. It ends with the list of `Sandcastle` implementation issues that build it.
- `docs/adr/0002-unattended-sandcastle-on-a-hosted-runner.md`: why Sandcastle runs unattended on a GitHub-hosted runner with a PAT that has no Workflows permission.

Resolves the map ticket [Task: Write the Sandcastle workflow spec and file the implementation issues](https://github.com/mpaulosky/Blazor-Server/issues/62) (the ticket is closed by hand with a resolution comment).

The first implementation issue is `Blocked by` this PR, so Sandcastle builds nothing until the spec is on `main`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

### Commits

- docs(sandcastle): Spec the enhanced Sandcastle workflow and record the hosted-runner ADR (`abf12d1`)
- docs(sandcastle): List the implementation issues in the workflow spec (`97bffc7`)
- docs(sandcastle): Address review on the workflow spec (`cc8f6b8`)
- docs(sandcastle): Address second review on the workflow spec (`017c7e1`)
- docs(sandcastle): File workflow-file changes as manual issues (`ac6c743`)
- docs(sandcastle): Address third review on the workflow spec (`61eb4f4`)
- docs(sandcastle): Address fourth review on the workflow spec (`5a3aa56`)
- docs(sandcastle): Count only pipeline-stopping failures toward the build cap (`c186dbb`)
- docs(sandcastle): Let only owner-approved content reach an agent (`24c55ce`)
- docs(sandcastle): Keep GitHub out of the sandbox and check label origin (`68a459e`)
- docs(sandcastle): Address sixth review on the workflow spec (`badfc6b`)
- docs(sandcastle): State the trust boundary and address seventh review (`98d1d2f`)
- docs(sandcastle): Keep the PAT out of the sandbox's git config (`1992ae0`)

### Files changed

#### docs/

- `docs/adr/0002-unattended-sandcastle-on-a-hosted-runner.md` (+41 / -0)
- `docs/plans/sandcastle-workflow.md` (+398 / -0)
